# --- Phase 0 -----------------------------------------------------------------
# Generate with:
#   -join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Max 256) })
GITHUB_WEBHOOK_SECRET=

# From https://smee.io  →  "Start a new channel"
SMEE_URL=

PORT=8787
LOG_LEVEL=info
DATA_DIR=.data

# --- Phase 1 (leave blank until Gates 1.1 - 1.3) ------------------------------
# DATABASE_URL=
# DATABASE_URL_DIRECT=
# GEMINI_API_KEY=
# GITHUB_TOKEN=
# TARGET_REPO=owner/name
